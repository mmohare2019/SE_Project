import { StyleSheet } from "react-native";

export default StyleSheet.create({
    centerView: {
        flex:1,
        alignItems:'center',
        justifyContent:'center' 
    },
    emphasisText: {
        fontSize: 40,
        color:'black',
        textAlign: 'center',
    },
    image: {
         marginBottom: 40,
    },
 });
